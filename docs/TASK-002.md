# TASK-002 — Create the immutable shared contract bundle

Implement the approved narrow bundle only: versioned schemas and IDs,
normalization, validation, descriptors/digests, negotiation, deterministic
migrations, and conformance fixtures. Do not add scheduler, worker, lifecycle,
authoring, review, sealing, or generic CRUD code. Use the Phase-1 frozen
inventory in `/lump/apps/specgen-aw/docs/research/` as source evidence. Provide
a Python wheel, minimal CLI/API, supported-version policy, fixtures, focused
tests, and a committed structured completion handoff.
